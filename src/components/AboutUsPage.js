import React from 'react';

const AboutUsPage = () => {
  return (
    <div className="min-h-screen bg-white">
      {/* Active Component will be rendered via Outlet in AboutLayout */}
    </div>
  );
};

export default AboutUsPage;